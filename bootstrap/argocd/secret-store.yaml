apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: argocd-local-gcpsm
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-local-gcpsm
    app.kubernetes.io/part-of: gke-bootstrap
spec:
  provider:
    gcpsm:
      auth:
        workloadIdentity:
          serviceAccountRef:
            name: argocd-secrets
